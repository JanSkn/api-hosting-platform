name: Setup Java and Maven
runs:
  using: "composite"
  steps:
    - uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: maven
    - name: Make wrapper executable
      run: chmod +x mvnw
      shell: bash