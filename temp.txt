!!!! für api gateway dann von frontend cloudfront url direkt an origin übergeben
AllowOrigin: !Sub "https://${FrontendCloudFront.DomainName}"

!! zwar eingestellt dass ecr nur letzten 3 images speichert aber dann muss in dynamo auch überschrieben werden

irgendwie tababstände unterschiedlich in den files selbst mit formatter?

ablauf cicd: ci ja nur unit tests vlt kleine integratino tests lintign etc
erstellt code zip, das nimmt staging und dann auch prod damit überall gleich
nach merge auf main staging deployen smoke tests machen integratino tests etc
!!! am besten so das is auch in reality eher so: staging startet alles automatisch und dann wenn alles grün prod 
deployment händisch wenn alles grün von staging und wenn deployed dann tag push
wichtig dass nur code owner das prod deployment approven können
staging integration test data wieder löschen? zb dynamo writes und so
zb in junit mit
@AfterEach
void cleanup() {
    // Lösche alle Test-Einträge für den dedizierten Test-User
    dynamoDbClient.deleteItem(builder -> builder
        .tableName("deployments-staging")
        .key(Map.of("userId", AttributeValue.builder().s("test-user-id").build()))
    );
}




!! resolve s3 bei sam is für zb lambda code, templates speichern etc
mit resolve s3 is halt automatisch sonst muss man bucket angeben
mit changes etc hat man dann aber verwaiste files !!

TODO checker dsas imports in rihctiger reihenfolge etc

github muss noch sam und aws installiern

in app.tsx amplify login test entfernen 

!! config.js auch ins gitignore? weil wird ja eh dann geschrieben von github action und is lokal anders

TODO überall per hand die region gesetzt und zb local deployment stack name auch per hand in toml und sh script

TODO dependency-reduced-pom unterdrücken?
TODO quarkus-amazon-lambda-rest nötig?

tests:
unit tests mit surefire (Test im dateinamen)
integration tests mit maven failsafe plugin (IT im dateinamen)

policies im sam template werden zu einer automaitsch erstellten rolle gebündelt


/Users/janskowron/VSCode/api-hosting-platform/lambda/core-backend-lambda/src/main/resources/application.properties
# Enable CORS support separately from API Gateway, as we use it in proxy mode and API Gateway will forward the CORS headers set by Quarkus to the client.
quarkus.http.cors=true
# TODO set specific origins depending on environment
quarkus.http.cors.origins=/.*/
# Allow the methods and headers needed for API Gateway integration
quarkus.http.cors.methods=GET, POST, PUT, DELETE, OPTIONS, HEAD, PATCH
quarkus.http.cors.headers=accept, authorization, content-type, x-requested-with
